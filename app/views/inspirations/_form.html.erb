<div class="form-style-6"> 

<%= form_for @inspiration, html: { multipart: true } do |f| %>
	<p><%= f.label :title, "Upload an Image" %>
		<%= f.file_field :image, required: true %>
	</p>
  <p><%= f.label :title %><br>
      <%= f.text_field :title %>
  </p>

  <p><%= f.label :body %><br>
      <%= f.text_area :body, rows: 10, col: 50 %>
  </p>

  <p><%= f.submit %></p>
<% end %>

</div>